const escape = value => String(value).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]);
export const template = (code, firstName, subject) => `<!doctype html><html><body style="font-family:Arial,sans-serif;max-width:560px;margin:40px auto;padding:24px"><h1>${escape(subject)}</h1><p>Hello ${escape(firstName)},</p><p>Your Sara7a verification code is:</p><p style="font-size:32px;letter-spacing:6px">${escape(code)}</p><p>This code expires in 10 minutes and can be used once. If you did not request it, ignore this email.</p><p>Sara7a</p></body></html>`;
